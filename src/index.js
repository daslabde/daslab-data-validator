import {
  validate,
  SUPPORTED_TYPES,
  SUPPORTED_RULES,
} from './validate/index'

import {
  boolean,
  date,
  email,
  number,
  phone,
  string,
} from './veeValidate/types'

import {
  dateFormat,
  length,
  max,
  maxValue,
  min,
  minValue,
  oneOf,
  required,
} from './veeValidate/rules'

export {
  validate,
  SUPPORTED_TYPES,
  SUPPORTED_RULES,

  boolean,
  date,
  email,
  number,
  phone,
  string,

  dateFormat,
  length,
  max,
  maxValue,
  min,
  minValue,
  oneOf,
  required,
}
