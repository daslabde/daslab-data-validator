import {
  validate,
  isValid,
  SUPPORTED_TYPES,
  SUPPORTED_RULES,
} from './validate/index'

import {
  boolean,
  date,
  email,
  number,
  phone,
  string,
} from './veeValidate/types'

import {
  length,
  max,
  maxValue,
  min,
  minValue,
  oneOf,
  required,
} from './veeValidate/rules'

export {
  validate,
  isValid,
  SUPPORTED_TYPES,
  SUPPORTED_RULES,

  boolean,
  date,
  email,
  number,
  phone,
  string,

  length,
  max,
  maxValue,
  min,
  minValue,
  oneOf,
  required,
}
