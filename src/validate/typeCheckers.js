const boolean = (rule) => {
  return rule === 'boolean'
}

const date = (rule) => {
  return rule === 'date'
}

const number = (rule) => {
  return rule === 'number'
}

const string = (rule) => {
  return rule === 'string'
}

export {
  boolean,
  date,
  number,
  string,
}
